{
  "name": "form-builder-schema-editor",
  "version": "1.0.0",
  "description": "Web-based schema editor for multi-step form configurations",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "form",
    "schema",
    "editor",
    "builder"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "esbuild": "^0.27.3"
  }
}
